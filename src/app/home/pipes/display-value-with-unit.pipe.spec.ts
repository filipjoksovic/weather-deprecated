import { DisplayValueWithUnitPipe } from './display-value-with-unit.pipe';

describe('DisplayValueWithUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayValueWithUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
